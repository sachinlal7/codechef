#include <stdio.h>


int main(){
	int n,a,b,i;
	scanf("%d",&n);
	int arr[n];
	for(i=0;i<n;i++)
	{
	scanf("%d %d",&a,&b);
	
	if(a+b<3)
	printf("1\n");
	else if(a+b>=3 && a+b<=10)
	printf("2\n");
	else if (a+b>=11 && a+b<=60)
	printf("3\n");
	else
	printf("4\n");	
	
	}
	return 0;
}
