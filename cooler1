#include <stdio.h>


void main()
{
	int i,n,x,y;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d%d",&x,&y);
		if(x>30*y)
		printf("YES\n");
		else if(x<30*y)
		printf("NO\n");
		else
		printf("YES\n");
	}
 return 0;
}
